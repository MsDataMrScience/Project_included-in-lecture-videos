x <- c(2, 43, 27, 96, 18)
sort(x)
order(x)
rank(x)
#numeric minimum value of a vector
min(x)
#index of a maximum value
which.max(x)
#numeric maximum value of a vector
max(x)
#index of a minimum value
which.min(x)

#Q3 for chapter 2
name <- c("Mandi", "Amy", "Nicole", "Olivia")
time <- c(10, 30, 40, 50)
#in R, arrithmatic operations occr element wise
time/60
time <- time/60
distance <- c(0.8, 3.1, 2.8, 4.0)
distance/time
speed <- distance/time
