log(1024,base=4)
#this is equal to following
log(1024,4)
#if not mentioned exclusively, 
#R assumes that base=1 needs to be calculated
#log(1024,1) = log(1024)
log(1024)
log(1024,1)
#nested functions to be solved inside out
log(exp(1)) # these are inverse functions
#function args gives arguments that function 
#log needs in order for this function to compute
#args (log)
function (x, base = exp(1))


