#code to compute a quadratic equation in the 
#form of
a(x*x)+bx +c = 0
#define the variables
a <- 3
b <- 2
c <- -1
#now compute the solution(2possible solutions:)
solution <- 1 (-b+sqrt(b^2-4*a*c))/(2*a)
solution <- 2 (-b-sqrt(b^2-4*a*c))/(2*a)

#all veriables saved in workspace can be found by
ls()
